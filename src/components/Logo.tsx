import React from 'react';

const Logo = () => {
    return (
        <div className="logo">
            <img src="logo.png" alt="Icon" className="logo__icon" />

        </div>
    );
};

export default Logo;
